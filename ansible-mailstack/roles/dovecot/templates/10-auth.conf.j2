# ============================================================
# Dovecot 2.4 Authentication
# Managed by Ansible
# ============================================================

auth_mechanisms        = plain login
auth_allow_cleartext   = no
auth_username_format   = %{user | lower}

# passdb + userdb blocks live in auth-sql.conf.ext
!include auth-sql.conf.ext
