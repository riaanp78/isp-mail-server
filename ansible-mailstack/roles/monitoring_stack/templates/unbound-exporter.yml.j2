# Managed by Ansible - Unbound Exporter Configuration

# Listen address and port
UNBOUND_EXPORTER_LISTEN=":9167"

# Unbound control socket (Unix socket)
UNBOUND_EXPORTER_HOST="unix:///run/unbound.ctl"

# Disable TLS (using Unix socket)
UNBOUND_EXPORTER_CA=""
UNBOUND_EXPORTER_CERT=""
UNBOUND_EXPORTER_KEY=""

