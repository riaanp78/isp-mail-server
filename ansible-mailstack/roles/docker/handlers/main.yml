---
- name: restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: yes
    
- name: reload docker
  ansible.builtin.systemd:
    name: docker
    state: reloaded
    daemon_reload: yes